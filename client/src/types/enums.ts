export const enum ProposalStatus {
    Draft = 0,
    Pending = 1,
    UnderReview = 2,
    Matched = 3,
    Active = 4,
    Submitted = 5,
    Completed = 6,
    Rejected = 7
}

export const enum MatchStatus {
    InterestExpressed = 0,
    Confirmed = 1,
    Cancelled = 2,
    Withdrawn = 3
}

export const enum MeetingStatus {
    Scheduled = 0,
    Completed = 1,
    Cancelled = 2
}
